import { DefaultProperties } from '@/features/json-form/types/default-properties';
import { DecisionSchema } from './schema';

export const defaultPropertiesData: DefaultProperties<DecisionSchema> = {
  label: 'node.decision.label',
  description: 'node.decision.description',
};
